import './initial-view.scss';

const InitialView = (props) => {
    return(
        <div className="welcome-box">
            Hi! Lets check what weather has in store for you ☝️
        </div> 
    );
}

export default InitialView;